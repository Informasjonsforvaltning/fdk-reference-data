query FindByURIs($req: FindByURIsRequest!) {
    findByURIs(req: $req) {
        uri
        code
        label {
            nb
            nn
            no
            en
        }
        type
    }
}
