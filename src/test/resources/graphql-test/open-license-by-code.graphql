query GetOpenLicenseByCode($code: String!) {
    openLicenseByCode(code: $code) {
        uri
        code
        isReplacedBy
        label {
            nb
            nn
            no
            en
        }
    }
}
