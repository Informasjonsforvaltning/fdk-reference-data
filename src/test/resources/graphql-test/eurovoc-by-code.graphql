query GetEuroVocByCode($code: String!) {
    euroVocByCode(code: $code) {
        uri
        code
        label {
            nb
            nn
            no
            en
        }
        parents
        children
    }
}
