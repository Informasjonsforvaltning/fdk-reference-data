query GetConceptStatusByCode($code: String!) {
    conceptStatusByCode(code: $code) {
        uri
        code
        label {
            nb
            nn
            no
            en
        }
    }
}
