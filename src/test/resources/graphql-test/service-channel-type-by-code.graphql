query ServiceChannelTypeRightByCode($code: String!) {
    serviceChannelTypeByCode(code: $code) {
        uri
        code
        label {
            nb
            nn
            no
            en
        }
    }
}
