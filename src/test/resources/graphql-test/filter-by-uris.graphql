query FilterByURIs($req: FilterByURIsRequest!) {
    filterByURIs(req: $req) {
        uri
        code
        label {
            nb
            nn
            no
            en
        }
        type
    }
}
