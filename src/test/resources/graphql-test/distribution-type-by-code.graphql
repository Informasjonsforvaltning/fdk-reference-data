query GetDistributionTypeByCode($code: String!) {
    distributionTypeByCode(code: $code) {
        uri
        code
        label {
            nb
            nn
            no
            en
        },
        startUse
    }
}
