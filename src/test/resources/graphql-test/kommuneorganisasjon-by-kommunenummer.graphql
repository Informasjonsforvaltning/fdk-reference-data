query GetKommuneOrganisasjonByKommunenummer($kommunenummer: String!) {
    kommuneOrganisasjonByKommunenummer(kommunenummer: $kommunenummer) {
        uri
        organisasjonsnummer
        kommunenummer
    }
}
