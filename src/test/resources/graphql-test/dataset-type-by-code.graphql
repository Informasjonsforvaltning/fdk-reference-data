query GetDatasetTypeByCode($code: String!) {
    datasetTypeByCode(code: $code) {
        uri
        code
        label {
            nb
            nn
            no
            en
        },
        startUse
    }
}
