query GetApiSpecificationByCode($code: String!) {
    apiSpecificationByCode(code: $code) {
        uri
        code
        source
        label {
            nb
            nn
            en
        }
    }
}
