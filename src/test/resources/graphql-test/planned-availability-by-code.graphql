query GetPlannedAvailabilityByCode($code: String!) {
    plannedAvailabilityByCode(code: $code) {
        uri
        code
        label {
            nb
            nn
            no
            en
        },
        startUse
    }
}
