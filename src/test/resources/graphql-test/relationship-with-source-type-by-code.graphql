query RelationshipWithSourceTypeRightByCode($code: String!) {
    relationshipWithSourceTypeByCode(code: $code) {
        uri
        code
        label {
            nb
            nn
            no
            en
        }
    }
}
